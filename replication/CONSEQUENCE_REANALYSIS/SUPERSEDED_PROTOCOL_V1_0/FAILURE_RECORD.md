# Superseded precomputation freeze record

The first computation attempt stopped before writing or printing any consequence
result. Its invariant check found that protocol V1.0 used the non-existent
machine literal `SUPPORTED_TEST_SOURCE_PRESENT`; the frozen V4.1 label file uses
`SOURCE_SUPPORTED_TEST_PRESENCE`.

Observed terminal message: `invalid frozen counts for CoreNLP`.

The formulas, population, endpoints, aggregation, uncertainty procedures, and
language boundaries were not changed. V1.1 corrects only the category literal.
The original V1.0 bytes and their SHA-256 receipt are preserved in this folder.
