# Paper04 N2 deterministic consequence reanalysis handoff

## Outcome

The exact anchor target was reconstructed, the 3,393-row join was identified,
the protocol was frozen before result exposure, and primary/independent exact
computations agreed. The bounded extension therefore passes for independent
science review.

The result is not a large correction of the published record. For the 14-project
downstream overlap, design plug-in pooled candidate-exclusion shares are
1.15–3.70%, and the all-commits pooled maximum upward envelopes are 0.16–0.52
percentage points. Project-level exposure is wider. Runtime outcomes remain
unidentified.

## Review order

1. `FINAL_STATUS.md`
2. `SCIENTIFIC_INTERPRETATION.md`
3. `ANCHOR_DOWNSTREAM_REPRODUCTION_RECEIPT.md`
4. `ANCHOR_FRAME_JOIN_AUDIT.md`
5. `DETERMINISTIC_REANALYSIS_PROTOCOL_FREEZE.md` and its SHA receipt
6. `DETERMINISTIC_CONSEQUENCE_RESULTS.md`
7. `INDEPENDENT_COMPACT_RECOMPUTE.json`
8. `CLAIM_EVIDENCE_MAP.md` and `FINAL_NONCLAIMS.md`
9. proposed R4.2 manuscript and SI patches

## Replay boundary

The final handoff contains the Paper04 frame/sample/labels and all analysis code.
The third-party anchor bytes are not redistributed; exact fixed pointers and
hashes are in `ANCHOR_SOURCE_PROVENANCE.md` and `INPUT_HASHES.json`. Replaying
the 66-project anchor gate requires downloading the official Zenodo
`processed-results.tar.gz` and fixed-commit `Many4JResults.csv`.

## Failed freeze preservation

The superseded V1.0 protocol and failure record are retained. The first result
launch stopped before writing or printing consequence values because of a
category-literal typo. V1.1 changed only that literal and was re-frozen before
successful execution.

## Boundaries

This package performs no Maven, repository, notebook, classifier, or test
execution; no source fetch beyond the frozen official anchor artifact; no human
coding claim; no public release; and no submission.

