# Final nonclaims

This reanalysis does not claim that:

- a source-supported snapshot would execute, pass, be reachable, or yield a
  positive parsed test count;
- the reported envelopes are corrected or true testability rates;
- unresolved snapshots contain tests;
- no-supported-source snapshots are semantically test-free;
- the anchor's conclusions are false, overturned, or re-adjudicated;
- the 2,279 rows in `canal`, `clojure`, `wildfly`, and `zheng` entered the
  published downstream summary;
- the 14-project overlap represents Maven, Java, GitHub, or software projects
  generally;
- design-based plug-in estimates, simultaneous confidence sets, and
  no-exchangeability bounds are interchangeable;
- either endpoint family has joint 95% coverage with the other;
- the post-exposure protocol is an untouched preregistration or independent
  human ground truth;
- R4.1, author metadata, Word compatibility, repository release, journal
  submission, or publication is authorized by this handoff.

